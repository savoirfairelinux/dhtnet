{
  "name": "dsh-ipv6-roundtrip",
  "description": "Run a WAN-side dhtnet dsh session over IPv6 and assert the echo roundtrip.",
  "required_inputs": [
    "run_id",
    "result_dir",
    "topology",
    "client_namespace",
    "actor_namespace",
    "actor_peer_id",
    "bootstrap_host",
    "bootstrap_port"
  ],
  "probe_sequence": [
    {
      "id": "namespace_list",
      "type": "capture_command",
      "label": "namespace list",
      "kind": "state-dump",
      "destination": "netns-list.txt",
      "argv": ["ip", "netns", "list"]
    },
    {
      "id": "namespaces_exist",
      "type": "assert_namespaces_exist",
      "namespaces": ["{client_namespace}", "{actor_namespace}"]
    },
    {
      "id": "client_ipv6_routes",
      "type": "capture_command",
      "label": "client IPv6 routes",
      "kind": "state-dump",
      "destination": "client-ipv6-routes.txt",
      "argv": ["ip", "-n", "{client_namespace}", "-6", "route", "show"]
    },
    {
      "id": "actor_ipv6_addresses",
      "type": "capture_command",
      "label": "actor IPv6 addresses",
      "kind": "state-dump",
      "destination": "actor-ipv6-addresses.txt",
      "argv": ["ip", "-n", "{actor_namespace}", "-6", "addr", "show"]
    },
    {
      "id": "actor_ipv6_route_to_bootstrap",
      "type": "capture_command",
      "label": "actor IPv6 route to bootstrap",
      "kind": "state-dump",
      "destination": "actor-ipv6-route-to-bootstrap.txt",
      "argv": [
        "ip",
        "-n",
        "{actor_namespace}",
        "-6",
        "route",
        "get",
        "{bootstrap_host}"
      ]
    },
    {
      "id": "wan_dsh_ipv6_roundtrip",
      "type": "dhtnet_dsh_roundtrip",
      "client_namespace": "{client_namespace}",
      "bootstrap_host": "{bootstrap_host}",
      "bootstrap_port": "{bootstrap_port}",
      "peer_id": "{actor_peer_id}",
      "capture": "wan-dsh-ipv6-client.txt",
      "timeout_s": 30,
      "startup_delay_s": 2,
      "output": "roundtrip_token"
    }
  ]
}
